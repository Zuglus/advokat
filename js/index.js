// 'use strict'

// const serviceButtonsList = document.querySelector(".service__buttons"); // список кнопок
// const container = document.querySelector(".wrapper");                 // ширина страницы

// function setButtons(list) {                                             // назначение шрифта и размеров кнопок услуг
//   const childrenList = list.children;
//   const baseWidth = list.offsetWidth
//   const newWidth = baseWidth / 4 + 'px';
//   const newFont = baseWidth / 40 - 2.5 + 'px';
//   for (let item of childrenList) {
//     item.style.width = newWidth;
//     item.style.fontSize = newFont;
//   }
// }

// window.addEventListener('load', () => {                                 // действия при загрузке страницы
//   if (container.offsetWidth > 320 && serviceButtonsList !== null) {
//     setButtons(serviceButtonsList);
//   }
// });

// window.addEventListener("resize", function () {                            // действия при изменении страницы
//   if (container.offsetWidth > 320 && serviceButtonsList !== null) {
//     setButtons(serviceButtonsList);
//   }
// });

